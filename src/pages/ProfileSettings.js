import React from 'react';
import PageTemplate from '../components/PageTemplate';

const ProfileSettings = () => {
  return (
    <PageTemplate title="Profile Settings" showFilter={false}>
      {/* Add your content here */}
    </PageTemplate>
  );
};

export default ProfileSettings;
