import React from 'react';
import PageTemplate from '../components/PageTemplate';

const AddAttributes = () => {
  return (
    <PageTemplate title="Add Attributes" showFilter={false}>
      {/* Add your content here */}
    </PageTemplate>
  );
};

export default AddAttributes;
