import React from 'react';
import PageTemplate from '../components/PageTemplate';

const AddBrand = () => {
  return (
    <PageTemplate title="Add Brand" showFilter={false}>
      {/* Add your content here */}
    </PageTemplate>
  );
};

export default AddBrand;
